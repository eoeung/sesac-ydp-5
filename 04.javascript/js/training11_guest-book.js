// [실습 11] 방명록 만들기
let number = 1;

const guestForm = document.querySelector('#guestForm');
guestForm.addEventListener('submit', function(e){
    e.preventDefault();

    console.log(1234);
    addGuestBook();
});

function addGuestBook(){
    const guest = document.querySelector('#guest');
    const content = document.querySelector('#content');
    const go = document.querySelector('#go');
    const guest_book = document.querySelector('#guest_book');

    const now = new Date();

    guest_book.
    
    number,
    guest.value,
    content.value,
    now.getFullYear(),
    now.getMonth()+1,
    now.getDay(),
    now.getHours(),
    now.getMinutes()
    )
}